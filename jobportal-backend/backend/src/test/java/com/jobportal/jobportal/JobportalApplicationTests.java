package com.jobportal.jobportal;

import org.junit.jupiter.api.Test;

// NOTE: @SpringBootTest removed intentionally - it requires a live DB connection
// Run the app with: mvn spring-boot:run -DskipTests
class JobportalApplicationTests {
    @Test
    void contextLoads() {
        // Tests skipped - run with: mvn spring-boot:run -DskipTests
    }
}
